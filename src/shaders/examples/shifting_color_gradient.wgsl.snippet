fn postprocess(in_color: vec4<f32>, in: VertexOutput, data: UiRenderData) -> vec4<f32> {
	var color_gradient = 0.5 + 0.5 * cos(data.time + vec3(in.tex_coords.xy, 0.0) + vec3(0, 2, 4));
	var color = in_color + (vec4(color_gradient .rgb, in_color.a) / 1.5);
	color.a = in_color.a;

	return color;
}